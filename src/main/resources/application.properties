spring.application.name=emotional-analytics

server.port=8080

#Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/emotional_analytics
spring.datasource.username=postgres
spring.datasource.password=Darren
spring.datasource.driver-class-name=org.postgresql.Driver

# Clerk JWKS URL
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://rational-grouse-21.clerk.accounts.dev/.well-known/jwks.json

# Disable open-in-view warning
spring.jpa.open-in-view=false

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
